# Supabase Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env files to version control

# Your Supabase project URL
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anon/public key (NOT the service role key!)
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Optional: Base URL for your application
VITE_BASE_URL=http://localhost:5173

# Optional: API URL if you have a separate backend
VITE_API_URL=http://localhost:3000

# ⚠️ IMPORTANT: 
# - Use ANON_KEY for frontend applications
# - NEVER use SERVICE_ROLE_KEY in frontend code
# - SERVICE_ROLE_KEY bypasses RLS and should only be used server-side
# - ANON_KEY respects RLS policies and is safe for frontend use

